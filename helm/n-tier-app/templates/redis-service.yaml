apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    {{- include "n-tier-app.componentLabels" (dict "component" "redis" "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/component: redis
  ports:
    - port: 6379
      targetPort: 6379
